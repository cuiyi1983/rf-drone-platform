version: '3.8'
services:
  platform:
    image: python:3.11-slim
    container_name: rf-drone-platform-test_platform
    working_dir: /repo
    command: sleep infinity
    volumes:
      - .:/repo
    ports:
      - "5100:5100"
      - "5101:5101"
      - "5102:5102"
      - "6103:6103"
    restart: unless-stopped
